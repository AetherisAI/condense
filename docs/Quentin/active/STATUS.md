- branch `claude/condense-access-status-tz7hpz` @ HEAD (this commit) — G3: closed the E2E v3 gate miss — `adapters/ocr/fallback_parser.py::OcrFallbackParser` was catching `Exception` broadly enough to also swallow a deliberate `core.errors.SiftError` (specifically the xlsx used-range guard's `ParseError`, D34), sending the rejected file through a pointless ~40s Mistral OCR round-trip that then 400'd on the xlsx bytes instead of failing fast with the guard's own clear message; reproduced 3× on real schedule `.xlsx` files. Fixed with `except SiftError: raise` ahead of the general `except Exception:` — deliberate rejections now propagate unchanged, zero OCR calls, regression-tested against the pre-existing fallback/pass-through cases. Also this round: `agent/client.py::SiftClient` default timeout 300s → 600s (a real OCR-heavy batch took 5m6s server-side) plus a `--timeout` CLI flag and matching `AgentConfig.timeout` field (backward-compatible `load()`, wired into `agent/app.py`); `agent/cli.py`'s `PARTIAL:` summary line now reports a `skipped` count (`skipped_dedup` results were silently dropped from the tally, inconsistent with `sync()`'s `Summary.line()` convention); `sift.config.Settings.embed_retry_attempts` / `parse_max_xlsx_cells` gained `Field(ge=1)` so `EMBED_RETRY_ATTEMPTS=0` fails clearly at construction instead of reaching an unhandled error mid-request. 195 tests green in a clean worktree (0 failed, was 186; +9), ruff check + format clean. Full context: `SESSION-HANDOFF.md` (G3 additions); Decision: `DECISIONS.md` **D36**. **CROSS-BOUNDARY:** edits Arthur-owned `adapters/ocr/fallback_parser.py` + `agent/{client,cli,config,app}.py` at Quentin's direction (same basis as D25/D29/D32/D34/D35) — flagged in `docs/channel/from-quentin.md` update 18. Pending: Arthur review; E2E v3 re-run to confirm the xlsx now fails fast+cleanly end to end; task #21's web-UI half (still open).
- (prior round) G2: closed three CLI/client accounting gaps found by the delta-audit. R2: `agent/cli.py::main` now catches `PartialIngestError` around the one-shot upload, printing every per-file status the server confirmed plus a `PARTIAL: X indexed, Y failed, Z of N never attempted` summary, and exits non-zero — previously an uncaught `PartialIngestError` printed a raw traceback, making a 14/20-landed run indistinguishable from a total failure. R3: `agent/client.py::SiftClient.ingest` had `body = r.json()` outside the POST's protected `try/except`, so a batch N>1 returning HTTP 200 with an invalid JSON body discarded every earlier batch's already-landed results instead of raising `PartialIngestError`; moved the decode inside the same block. R4: `agent/sync.py`'s walk now prunes vendored/tooling directories (`.venv`, `node_modules`, `.dist-info`/`.egg-info`, …) via a new `DEFAULT_EXCLUDE_DIRS` frozenset, overridable via `AgentConfig.exclude_dirs` / `agent/cli.py --exclude-dir` — closes the Acme corpus finding (numpy/lxml license files nested under `ACME-TOOLING/.venv/lib/site-packages/*.dist-info` inflating the upload set). R5: `.env.example` + README §8 gained the round-2/3 Settings keys (`EMBED_BATCH_SIZE`/`EMBED_TIMEOUT_S`/`EMBED_CONNECT_TIMEOUT_S`/`EMBED_RETRY_ATTEMPTS`, `OCR_TIMEOUT_S`/`OCR_CONNECT_TIMEOUT_S`, `PARSE_MAX_XLSX_CELLS`, `VERSION_COLLAPSE_ENABLED`/`VERSION_SIMILARITY_THRESHOLD`) that were missing from both. 186 tests green in a clean worktree (0 failed, was 179; +7), ruff check + format clean (one pre-existing, unrelated diff remains in Arthur's `libsql.py`). Full context: `SESSION-HANDOFF.md` (G2 additions). Pending: Arthur review of `agent/*.py` (cross-boundary touch, same basis as D25/D29/D32/D34); E2E v3; task #21's web-UI half (still open).
- (prior round) G1: isolated + root-caused the E2E v2 parser blowup to one real 38KB `.xlsx` whose stray far cell inflated its declared used-range to ~44M cells, which markitdown's `pandas.read_excel(engine="openpyxl")` tried to materialize (measured: 1.85GiB+ RSS, cgroup-OOM-killed, ~140s); fixed with a cheap pre-parse dimension guard in `adapters/parsing/markitdown.py` (`ParseError`, config-driven `parse_max_xlsx_cells`, zero pipeline changes needed) — post-fix re-repro: same file now fails in 0.00s at 146MB RSS (D34). Also this round: bounded HTTP 429 retry for the embedder (TEI's per-input-string permit limit, config-driven `embed_retry_attempts`), and `scripts/run-engine.sh` drops its `MemoryHigh` band (this session's own repro of that exact livelock — anon-only + zero-swap + a throttle band — is why it's gone; `MemoryMax`-only everywhere now). 179 tests green in a clean worktree (0 failed, was 172; +7), ruff check + format clean (one pre-existing, unrelated diff remains in Arthur's `libsql.py`).
- (prior round) agent fresh-DB fix + image-OCR + version-collapse (D27) + true-mtime recency (D28) + RAM-containment/local-TEI-embeddings (D29/D30) + `/status` ocr_api_key redaction fix + engine resilience: bounded config-driven embed/OCR timeouts, configurable embed batch size, per-file ingest audit logging (D31) + agent memory bound (streamed hash + lazy per-batch reads + size guard), partial-batch accounting, watcher regression tests (D32) + trustworthy recency: validated mtimes at the `/ingest` boundary, real-datetime comparison in `_is_newer`, known-beats-unknown collapse rule (D33). 172 tests green in a clean worktree (0 failed, was 167; +5 for D33), ruff check + format clean (one pre-existing, unrelated diff remains in Arthur's `libsql.py`). Live-validated with real Mistral + libSQL (D27–D30 era; D31–D33 unit/integration only so far).
- (historical) WP0 feat/contracts: 994dbd4 — contracts (core types+ports, api schemas) + fakes (FakeEmbedder/FakeVectorStore/NullReranker) + compose skeleton — tests: 11 passed, ruff clean
