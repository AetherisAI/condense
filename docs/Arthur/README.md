# Arthur — Engine docs

> Dev A's planning + decisions land here (ingest, libSQL store, parsing, chunking, agent).
Shared source of truth: this sits beside `docs/Quentin/` so we can see both halves stay aligned.
