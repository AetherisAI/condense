{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core defaults, dialog defaults (folder picker for the ingest agent), and shell access scoped to the sift-agent-cli sidecar the app supervises — no unscoped shell execute/open.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    {
      "identifier": "shell:allow-spawn",
      "allow": [{ "name": "binaries/sift-agent-cli", "sidecar": true, "args": true }]
    },
    "shell:allow-kill"
  ]
}
